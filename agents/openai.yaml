interface:
  display_name: "Photo Alchemy"
  short_description: "Turn ordinary photos into distinctive source-aware art"
  default_prompt: "Use $photo-alchemy to transform this photo. Default to extend unless I explicitly request abstraction, reduction, or multiple methods."
